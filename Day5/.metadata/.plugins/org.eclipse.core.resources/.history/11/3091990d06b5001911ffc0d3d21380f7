package com.hcl.collections;

public class BasicComparator {
	/**
	   * compare() .
	   * @param o1 .
	   * @param o2 .
	   */

	  @Override
	  public int compare(Object o1, Object o2) {
	    Employ e1 = (Employ)o1;
	    Employ e2 = (Employ)o2;
	    return e1.basic.compareTo(e2.basic);
	  }
}
